{% extends "base.j2" %}
{% block content %}
<div class="main">
<h1>Locations</h1>
    <div class="container data_table">
        <table class="table">
            <thead>
                <tr>
                    <th>id</th>
                    <th>address</th>
                    <th>city</th>
                    <th>state</th>
                    <th>zip code</th>
                    <th>car qty</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            {% for loc in Locations %}
                <tr>
                {% for key in loc.keys() %}
                    <td>{{loc[key]}}</td>
                {% endfor %}
                    <td><a href="Locations/update/{{loc['location_id']}}">Edit</a></td>
                    <td><a href="Locations/delete/{{loc['location_id']}}" onclick="return confirm('Delete {{loc['city']}} from your records? This action is irreversible.')">Delete</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="container data_forms">
        <div class="form_div">
            <form class="form" method="POST" action="/Locations" id="addLocation">
                <label for="cars">Add a Location:</label>
                <fieldset class="form__field">
                    <div class="field__item">
                        <label> Address </label>
                        <input type="text" class="field__input" name="address" required>
                    </div>
                    <div class="field__item">
                        <label> City </label>
                        <input type="text" class="field__input" name="city" required>
                    </div>
                    <div class="field__item">
                        <label> State </label>
                        <input type="text" class="field__input" name="state" required>
                    </div>
                    <div class="field__item">
                        <label> Zipcode </label>
                        <input type="number" class="field__input" name="zipcode" required>
                    </div>
                    <div class="field__item">
                        <label> Number of Cars </label>
                        <input type="number" class="field__input" name="qty" required>
                    </div>
                    <input type="submit" value="Add" name="add_location" class="field__button">
                </fieldset>
            </form>
        </div>
        <div class="form_div">
            <form class="form" method="POST" action="/Locations" id="searchLocation">
                <label for="cars">Filter locations by city:</label>
                <fieldset class="form__field">
                    <div class="field__item">
                        <label> City </label>
                        <select class="field__input" name="city">
                        {% for city in cities %}
                            <option value="{{city['city']}}">{{city['city']}}</option>
                        {% endfor %}
                        </select>
                    </div>
                    <input type="submit" value="Search" name="search_location" class="field__button">
                    <input type="submit" value="Reset" name="reset_search" class="field__button">
                </fieldset>
            </form>
        </div>
    </div>
</div>
{% endblock %}
